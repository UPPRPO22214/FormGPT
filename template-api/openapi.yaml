openapi: 3.0.3
info:
  title: FormGPT API
  version: "0.1.0"
  description: API для конструктора опросов с GPT помощником

servers:
  - url: http://survey-service:8080

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    User:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
        createdAt:
          type: string
          format: date-time

    CreateSurveyRequest:
      type: object
      required:
        - title
      properties:
        title:
          type: string
        description:
          type: string
        questions:
          type: array
          items:
            $ref: "#/components/schemas/CreateQuestionRequest"

    SurveyResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        ownerId:
          type: string
          format: uuid
        questions:
          type: array
          items:
            $ref: "#/components/schemas/QuestionResponse"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateQuestionRequest:
      type: object
      required:
        - title
        - type
      properties:
        id:
          type: integer
          nullable: true
          description: |
            ID существующего вопроса для обновления.
            Если null или отсутствует - создается новый вопрос.
        title:
          type: string
        type:
          type: string
          enum: [single-choice, multiple-choice, text, scale-1-10]
        options:
          type: array
          items:
            type: string

    QuestionResponse:
      type: object
      properties:
        id:
          type: integer
        surveyId:
          type: integer
        title:
          type: string
        type:
          type: string
          enum: [single-choice, multiple-choice, text, scale-1-10]
        options:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    Answer:
      type: object
      properties:
        questionId:
          type: integer
        value:
          type: string

    SurveyWithAnswersResponse:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        ownerId:
          type: string
          format: uuid
        hasUserResponded:
          type: boolean
          description: Ответил ли текущий пользователь на этот опрос
        userRespondedAt:
          type: string
          format: date-time
          nullable: true
        questions:
          type: array
          items:
            $ref: "#/components/schemas/QuestionWithAnswerResponse"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    QuestionWithAnswerResponse:
      type: object
      properties:
        id:
          type: integer
        surveyId:
          type: integer
        title:
          type: string
        type:
          type: string
          enum: [single-choice, multiple-choice, text, scale-1-10]
        options:
          type: array
          items:
            type: string
        userAnswer:
          type: string
          description: Ответ текущего пользователя на этот вопрос
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    SurveyAnswer:
      type: object
      properties:
        answers:
          type: array
          items:
            $ref: "#/components/schemas/Answer"

    GPTSurveyCreateRequest:
      type: object
      required:
        - description
        - questionCount
      properties:
        description:
          type: string
          description: |
            Описание темы опроса. Будет использовано GPT для генерации релевантных вопросов.
            Примеры:
            - "Опрос об удовлетворенности сотрудников"
            - "Исследование предпочтений пользователей мобильного приложения"
          example: "Опрос о продуктивности удаленной работы"
        targetAudience:
          type: string
          description: |
            Целевая аудитория опроса (необязательно).
            Помогает GPT адаптировать формулировки вопросов.
            Примеры: "студенты", "сотрудники IT-компаний", "родители детей 3-6 лет"
          example: "офисные сотрудники"
          nullable: true
        questionCount:
          type: integer
          description: |
            Количество вопросов в генерируемом опросе.
            Минимум 1, максимум 20.
          minimum: 1
          maximum: 20
          example: 5

    GPTAddQuestionRequest:
      type: object
      properties:
        topic:
          type: string
          description: |
            Тематика для нового вопроса (необязательно).
            Если не указана, будет использована тема опроса.
          example: "Баланс работы и личной жизни"
          nullable: true
        targetAudience:
          type: string
          description: |
            Целевая аудитория для нового вопроса (необязательно).
            Примеры: "менеджеры", "разработчики", "новички в команде"
          example: "удаленные работники"
          nullable: true

    GPTQuestionImproveRequest:
      type: object
      properties:
        prompt:
          type: string
          description: |
            Дополнительные инструкции для GPT по улучшению вопроса (необязательно).
            Примеры:
            - "Сделай вопрос более понятным для новичков"
            - "Добавь больше вариантов ответа"
            - "Переформулируй в более формальном стиле"
          example: "Сделай варианты ответа более разнообразными"
          nullable: true

    GPTAnalysis:
      type: object
      properties:
        text:
          type: string
        score:
          type: string
          enum: [good, average, bad]

    SurveyAnalytics:
      type: object
      properties:
        surveyId:
          type: integer
        title:
          type: string
        description:
          type: string
        totalRespondents:
          type: integer
          description: Общее количество респондентов
        completedCount:
          type: integer
          description: Количество завершенных опросов
        incompletedCount:
          type: integer
          description: Количество незавершенных опросов
        questionsAnalytics:
          type: array
          items:
            $ref: "#/components/schemas/QuestionAnalytics"
        gptAnalysis:
          type: string
          nullable: true
          description: Анализ от GPT (генерируется по запросу)

    QuestionAnalytics:
      type: object
      properties:
        questionId:
          type: integer
        questionTitle:
          type: string
        questionType:
          type: string
          enum: [single-choice, multiple-choice, text, scale-1-10]
        totalAnswers:
          type: integer
          description: Количество ответов на этот вопрос
        answerDistribution:
          oneOf:
            - $ref: "#/components/schemas/ChoiceDistribution"
            - $ref: "#/components/schemas/ScaleDistribution"
            - $ref: "#/components/schemas/TextAnalytics"
          description: Распределение ответов в зависимости от типа вопроса

    ChoiceDistribution:
      type: object
      properties:
        options:
          type: array
          items:
            type: string
        counts:
          type: array
          items:
            type: integer
          description: Количество выборов для каждого варианта
        percentages:
          type: array
          items:
            type: number
            format: float
          description: Процентное соотношение для каждого варианта

    ScaleDistribution:
      type: object
      properties:
        min:
          type: integer
        max:
          type: integer
        average:
          type: number
          format: float
        distribution:
          type: array
          items:
            type: integer
          description: Количество ответов для каждой оценки (1-10)
        median:
          type: number
          format: float

    TextAnalytics:
      type: object
      properties:
        totalAnswers:
          type: integer
        wordCloud:
          type: array
          items:
            type: string
          description: Наиболее частые слова/фразы
        sampleAnswers:
          type: array
          items:
            type: string
          description: Примеры ответов (первые 5)

    GPTAnalyticsRequest:
      type: object
      properties:
        includeGPT:
          type: boolean
          default: false
          description: Включить ли GPT-анализ в ответ

    ErrorResponse:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
          example: "2025-12-07T22:49:18.402695"
        status:
          type: integer
          example: 400
        error:
          type: string
          example: "Bad Request"
        message:
          type: string
          example: "Invalid request parameters"
        path:
          type: string
          example: "/api/gpt/surveys/create"

security:
  - bearerAuth: []

paths:
  /health:
    get:
      summary: Проверка работы сервиса
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

  /auth/register:
    post:
      summary: Регистрация пользователя
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                password:
                  type: string
      responses:
        '200':
          description: Успешная регистрация
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string

  /auth/login:
    post:
      summary: Логин
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                password:
                  type: string
      responses:
        '200':
          description: Успешная авторизация
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string

  /users/me:
    get:
      summary: Профиль пользователя
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Данные пользователя
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /surveys:
    get:
      summary: Список всех своих опросов
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Список опросов
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SurveyResponse"

    post:
      summary: Создать новый опрос
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSurveyRequest"
      responses:
        '201':
          description: Опрос создан
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SurveyResponse"

  /surveys/{surveyId}:
    get:
      summary: Получить конкретный опрос
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: surveyId
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Опрос
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SurveyWithAnswersResponse"

    put:
      summary: Обновить опрос
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: surveyId
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSurveyRequest"
      responses:
        '200':
          description: Обновлённый опрос
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SurveyResponse"

    delete:
      summary: Удалить опрос
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: surveyId
          required: true
          schema:
            type: integer
      responses:
        '204':
          description: Успешно удалён

  /surveys/{surveyId}/answers:
    post:
      summary: Отправка ответов на опрос
      description: |
        Отправляет ответы на опрос. 
        Если пользователь уже отвечал на этот опрос - его предыдущие ответы обновляются.
        
        🔍 **Проверки:**
        - Обязательные вопросы должны быть заполнены
        - Для single-choice ответ должен быть одним из предложенных вариантов
        - Для scale-1-10 ответ должен быть числом от 1 до 10
        - Для text ответ не должен превышать 1000 символов
        
        ✅ **Особенности:**
        - При повторной отправке ответов старые ответы заменяются новыми
        - вопросы можно пропускать (value = null)
        - Для multiple-choice передавайте варианты через `;`
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: surveyId
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SurveyAnswer"
      responses:
        '200':
          description: Ответы успешно сохранены или обновлены
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "success"

  /gpt/surveys/create:
    post:
      tags:
        - GPT
      summary: Создание черновика опроса через GPT
      description: |
        Генерирует полный опрос с заданным количеством вопросов на указанную тему.
        
        Процесс:
        1. GPT анализирует описание темы
        2. Создает заголовок опроса
        3. Генерирует указанное количество вопросов разных типов
        4. Возвращает готовый к сохранению опрос
        
        ⚠️ **Ограничения:**
        - Максимум 20 вопросов за один запрос
        - Может занять до 30 секунд
        - Требует авторизации
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GPTSurveyCreateRequest"
            examples:
              basicExample:
                summary: Базовый пример
                value:
                  description: "Удовлетворенность условиями работы в офисе"
                  questionCount: 5
              detailedExample:
                summary: Пример с целевой аудиторией
                value:
                  description: "Исследование привычек использования смартфона"
                  targetAudience: "молодежь 18-25 лет"
                  questionCount: 8
      responses:
        '200':
          description: |
            Успешно сгенерированный опрос.
            Опрос сохранен в системе и готов к редактированию или публикации.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SurveyResponse"
              examples:
                successExample:
                  summary: Пример успешного ответа
                  value:
                    id: 42
                    title: "Опрос об условиях работы в офисе"
                    description: "Сгенерировано GPT"
                    ownerId: "e4947280-7093-4d5f-8e2e-da25f818bbf8"
                    questions:
                      - id: 101
                        surveyId: 42
                        title: "Насколько вам комфортна температура в офисе?"
                        type: "single-choice"
                        options: ["Очень комфортно", "Комфортно", "Нейтрально", "Дискомфортно", "Очень дискомфортно"]
                        createdAt: "2025-12-07T22:49:18.402695"
                        updatedAt: "2025-12-07T22:49:18.402695"
                      - id: 102
                        surveyId: 42
                        title: "Какие из этих удобств доступны в вашем офисе?"
                        type: "multiple-choice"
                        options: ["Кофе-машина", "Фрукты", "Спортивная комната", "Места для отдыха", "Парковка"]
                        createdAt: "2025-12-07T22:49:18.402695"
                        updatedAt: "2025-12-07T22:49:18.402695"
                    createdAt: "2025-12-07T22:49:18.402695"
                    updatedAt: "2025-12-07T22:49:18.402695"
        '400':
          description: |
            Некорректные параметры запроса.
            - description не может быть пустым
            - questionCount должен быть от 1 до 20
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '401':
          description: Требуется авторизация
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '500':
          description: Ошибка при обращении к GPT сервису
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /gpt/surveys/{surveyId}/questions:
    post:
      tags:
        - GPT
      summary: Добавление нового вопроса в существующий опрос через GPT
      description: |
        Генерирует и добавляет новый вопрос в указанный опрос.
        
        Особенности:
        - Если не указана тема (topic), используется тема опроса
        - Порядковый индекс вопроса рассчитывается автоматически
        - Поддерживаются все типы вопросов (single-choice, multiple-choice, text, scale)
        - Пользователь должен быть создателем опроса
        
        ✅ **Используйте когда:** нужно расширить опрос новыми вопросами без ручного написания.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: surveyId
          required: true
          description: ID существующего опроса
          schema:
            type: integer
            example: 42
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GPTAddQuestionRequest"
            examples:
              withTopic:
                summary: С указанием темы
                value:
                  topic: "Работа в команде"
                  targetAudience: "разработчики"
              withoutTopic:
                summary: Без указания темы
                value:
                  targetAudience: "менеджеры проектов"
      responses:
        '201':
          description: |
            Вопрос успешно сгенерирован и добавлен в опрос.
            Возвращает созданный вопрос с присвоенным ID.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuestionResponse"
              examples:
                createdQuestion:
                  summary: Пример созданного вопроса
                  value:
                    id: 150
                    surveyId: 42
                    title: "Насколько эффективно проходит коммуникация в вашей команде?"
                    type: "single-choice"
                    options:
                      - "Очень эффективно"
                      - "Достаточно эффективно"
                      - "Нейтрально"
                      - "Малоэффективно"
                      - "Совсем неэффективно"
                    createdAt: "2025-12-07T22:49:18.402695"
                    updatedAt: "2025-12-07T22:49:18.402695"
        '400':
          description: Некорректный запрос
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '401':
          description: Требуется авторизация
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '403':
          description: Нет прав на редактирование опроса
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '404':
          description: Опрос не найден
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '500':
          description: Ошибка при генерации вопроса
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /gpt/questions/{questionId}/edit:
    put:
      tags:
        - GPT
      summary: Улучшение существующего вопроса через GPT
      description: |
        Улучшает формулировку, варианты ответов или структуру существующего вопроса с помощью GPT.
        
        Что можно улучшить:
        - Формулировку вопроса (сделать яснее/проще)
        - Варианты ответов (добавить/удалить/изменить)
        - Тип вопроса (например, преобразовать single-choice в multiple-choice)
        
        💡 **Совет:** используйте поле `prompt` для конкретных инструкций по улучшению.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: questionId
          required: true
          description: ID вопроса для улучшения
          schema:
            type: integer
            example: 123
      requestBody:
        required: false
        description: Дополнительные инструкции для GPT (необязательно)
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GPTQuestionImproveRequest"
            examples:
              withPrompt:
                summary: С конкретными инструкциями
                value:
                  prompt: "Сделай вопрос более понятным для людей без технического образования"
              withoutPrompt:
                summary: Без инструкций (GPT улучшит на свое усмотрение)
                value: {}
      responses:
        '200':
          description: |
            Вопрос успешно улучшен.
            Возвращает обновленный вопрос.
            
            ⚠️ **Внимание:** изменения сохраняются в базе данных.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuestionResponse"
              examples:
                improvedQuestion:
                  summary: Пример улучшенного вопроса
                  value:
                    id: 123
                    surveyId: 42
                    title: "Насколько удобен интерфейс системы для неопытных пользователей?"
                    type: "single-choice"
                    options:
                      - "Очень удобен, все интуитивно понятно"
                      - "В целом удобен, но есть сложности"
                      - "Затрудняюсь ответить"
                      - "Сложноват, нужно время чтобы разобраться"
                      - "Совсем неудобен, все запутанно"
                    createdAt: "2025-12-07T22:49:18.402695"
                    updatedAt: "2025-12-07T23:15:30.123456"
        '400':
          description: Некорректный запрос
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '401':
          description: Требуется авторизация
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '403':
          description: Нет прав на редактирование вопроса
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '404':
          description: Вопрос не найден
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        '500':
          description: Ошибка при улучшении вопроса
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /gpt/surveys/{surveyId}/analyze:
    post:
      summary: Анализ качества опроса через GPT
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: surveyId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Рекомендации и оценка опроса
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GPTAnalysis"

  /surveys/{surveyId}/analytics:
    get:
      summary: Получение аналитики по опросу для дашборда
      description: |
        Возвращает статистику по ответам на опрос.
        
        📊 **Возвращаемые данные:**
        - Общее количество респондентов
        - Количество завершенных/незавершенных опросов
        - Для каждого вопроса:
          * Распределение ответов (в зависимости от типа вопроса)
          * Количество ответов
          * Процентное соотношение для вариантов выбора
          * Средние значения для шкалы 1-10
          * Примеры текстовых ответов
        
        🤖 **GPT-анализ (опционально):**
        При запросе с параметром includeGPT=true - дополнительно запрашивает у GPT
        сводный анализ результатов опроса с выводами и рекомендациями.
      tags:
        - Analytics
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: surveyId
          required: true
          schema:
            type: integer
        - in: query
          name: includeGPT
          required: false
          schema:
            type: boolean
            default: false
          description: Включить ли GPT-анализ результатов
      responses:
        '200':
          description: Статистика опроса
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SurveyAnalytics"
              examples:
                basicStats:
                  summary: Базовая статистика
                  value:
                    surveyId: 42
                    title: "Опрос об условиях работы"
                    description: "Анонимный опрос сотрудников"
                    totalRespondents: 156
                    completedCount: 142
                    incompletedCount: 14
                    questionsAnalytics:
                      - questionId: 101
                        questionTitle: "Насколько вам комфортна температура в офисе?"
                        questionType: "single-choice"
                        totalAnswers: 142
                        answerDistribution:
                          options: ["Очень комфортно", "Комфортно", "Нейтрально", "Дискомфортно", "Очень дискомфортно"]
                          counts: [15, 45, 52, 20, 10]
                          percentages: [10.6, 31.7, 36.6, 14.1, 7.0]
                      - questionId: 102
                        questionTitle: "Какие из этих удобств доступны в вашем офисе?"
                        questionType: "multiple-choice"
                        totalAnswers: 142
                        answerDistribution:
                          options: ["Кофе-машина", "Фрукты", "Спортивная комната", "Места для отдыха", "Парковка"]
                          counts: [142, 89, 15, 67, 45]
                          percentages: [100.0, 62.7, 10.6, 47.2, 31.7]
                      - questionId: 103
                        questionTitle: "Оцените общую удовлетворенность от 1 до 10"
                        questionType: "scale-1-10"
                        totalAnswers: 142
                        answerDistribution:
                          min: 1
                          max: 10
                          average: 6.8
                          distribution: [2, 5, 8, 12, 18, 25, 30, 20, 15, 7]
                          median: 7.0
                      - questionId: 104
                        questionTitle: "Что бы вы улучшили в офисе?"
                        questionType: "text"
                        totalAnswers: 112
                        answerDistribution:
                          totalAnswers: 112
                          wordCloud: ["освещение", "столы", "стулья", "шум", "кухня", "температура"]
                          sampleAnswers: 
                            - "Добавить больше розеток"
                            - "Улучшить освещение в коридорах"
                            - "Купить более удобные стулья"
                    gptAnalysis: null
                withGPT:
                  summary: Со статистикой от GPT
                  value:
                    # ... те же данные что выше ...
                    gptAnalysis: |
                      На основе анализа 142 завершенных опросов можно сделать следующие выводы:
                      
                      1. **Температурный режим**: Большинство сотрудников (73%) оценивают температуру как комфортную или нейтральную,
                      но 21% испытывают дискомфорт. Рекомендуется рассмотреть возможность регулировки температуры.
                      
                      2. **Удобства**: Кофе-машина есть у всех, но только 11% имеют доступ к спортивной комнате.
                      Это может быть потенциальным направлением для улучшения условий.
                      
                      3. **Общая удовлетворенность**: Средний балл 6.8/10 показывает удовлетворительный, но не идеальный уровень.
                      Распределение ответов близко к нормальному с пиком на 7 баллах.
                      
                      4. **Предложения по улучшению**: Чаще всего сотрудники упоминают освещение, мебель и уровень шума.
                      
                      **Рекомендации**: 
                      - Проверить и отрегулировать температуру в зонах с жалобами
                      - Рассмотреть организацию спортивной зоны
                      - Собрать фокус-группу по вопросам освещения и мебели
        '401':
          description: Требуется авторизация
        '403':
          description: Нет доступа к этому опросу
        '404':
          description: Опрос не найден

  /surveys/{surveyId}/export/csv:
    get:
      summary: Экспорт результатов опроса в CSV
      description: |
        Генерирует и возвращает CSV файл с результатами опроса.
        
        📋 **Формат CSV:**
        - Первая строка: заголовки (вопросы)
        - Последующие строки: ответы каждого респондента
        - Для multiple-choice ответы объединяются через точку с запятой
        - Пустые ячейки для пропущенных вопросов
        
        ⚡ **Особенности:**
        - Поддерживает кириллицу (UTF-8 encoding)
        - Автоматически экранирует запятые и кавычки в текстовых ответах
        - Включает метаданные респондента (ID, дата ответа, статус завершения)
        - Можно выбрать формат: с ответами в строках или колонках
        
        ⬇️ **Пример использования:** 
        ```javascript
        // Frontend загрузка файла
        fetch(`/surveys/${surveyId}/export/csv`)
          .then(response => response.blob())
          .then(blob => {
            const url = window.URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = `survey-${surveyId}-results.csv`;
            a.click();
          });
        ```
      tags:
        - Export
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: surveyId
          required: true
          schema:
            type: integer
        - in: query
          name: format
          required: false
          schema:
            type: string
            enum: [by-respondent, by-question]
            default: by-respondent
          description: |
            Формат вывода:
            - by-respondent (по умолчанию): каждая строка - ответы одного респондента
            - by-question: каждая строка - распределение ответов на один вопрос
        - in: query
          name: includeMetadata
          required: false
          schema:
            type: boolean
            default: true
          description: Включать ли метаданные (ID респондента, дата, статус)
        - in: query
          name: dateFrom
          required: false
          schema:
            type: string
            format: date
          description: Фильтр по дате начала (YYYY-MM-DD)
        - in: query
          name: dateTo
          required: false
          schema:
            type: string
            format: date
          description: Фильтр по дате окончания (YYYY-MM-DD)
      responses:
        '200':
          description: CSV файл с результатами
          headers:
            Content-Disposition:
              schema:
                type: string
              example: 'attachment; filename="survey-42-results-2025-12-08.csv"'
          content:
            text/csv:
              schema:
                type: string
                format: binary
              examples:
                byRespondent:
                  summary: Формат "по респонденту"
                  value: |
                    respondent_id,completion_status,response_date,Насколько вам комфортна температура в офисе?,Какие из этих удобств доступны в вашем офисе?,Оцените общую удовлетворенность от 1 до 10,Что бы вы улучшили в офисе?
                    123e4567-e89b-12d3-a456-426614174000,COMPLETED,2025-12-08T10:30:00,Комфортно,"Кофе-машина;Фрукты",8,"Добавить больше розеток"
                    123e4567-e89b-12d3-a456-426614174001,COMPLETED,2025-12-08T11:15:00,Нейтрально,"Кофе-машина",6,Улучшить освещение
                    123e4567-e89b-12d3-a456-426614174002,INCOMPLETE,2025-12-08T12:00:00,Очень комфортно,"Кофе-машина;Фрукты;Места для отдыха",,,
                byQuestion:
                  summary: Формат "по вопросу"
                  value: |
                    question_id,question_text,question_type,response_option,count,percentage
                    101,Насколько вам комфортна температура в офисе?,single-choice,Очень комфортно,15,10.6%
                    101,Насколько вам комфортна температура в офисе?,single-choice,Комфортно,45,31.7%
                    101,Насколько вам комфортна температура в офисе?,single-choice,Нейтрально,52,36.6%
                    102,Какие из этих удобств доступны в вашем офисе?,multiple-choice,Кофе-машина,142,100.0%
                    102,Какие из этих удобств доступны в вашем офисе?,multiple-choice,Фрукты,89,62.7%
        '401':
          description: Требуется авторизация
        '403':
          description: Нет доступа к этому опросу
        '404':
          description: Опрос не найден
        '500':
          description: Ошибка при генерации CSV файла